import Layout from "../components/Layout";

const Route = () => {
  const titulo = <h1>Navegação</h1>

  return (
    <Layout titulo="Navegação#01">
      <div>
        {titulo}
      <p></p>
    </div>
    </Layout>
  )
}

export default Route;
